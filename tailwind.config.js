/** @type {import('tailwindcss').Config} */
module.exports = {
    content: [
        './**/*.html',
    ],
    theme: {
        fontFamily: {
            sans: ['FiraSans', 'sans-serif'],
            serif: ['serif'],
            mono: ['monospace'],
        },
    },
}