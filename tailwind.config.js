/** @type {import('tailwindcss').Config} */
module.exports = {
    content: [
        './**/*.html',
    ],
    theme: {
        fontFamily: {
            sans: ['Roboto', 'system-ui', 'sans-serif'],
            serif: ['serif'],
            mono: ['monospace'],
        },
    },
}